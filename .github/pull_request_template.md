## Summary

<!-- What changed and why. Reference the issue this PR addresses, if any. -->

## Validation

<!-- Commands run and their results, e.g. `dotnet build`, `dotnet test ...`. -->

## Checklist

- [ ] If this PR contains a breaking change (a removed/renamed public API, a
      changed default behavior, or a removed `[Obsolete]` member), add a
      "Breaking changes" entry to [`CHANGELOG.md`](../CHANGELOG.md) under the
      version this PR ships as (or under `## Unreleased` if unsure of the
      exact version).
- [ ] If this PR adds, removes, or changes a documented API, update the
      relevant doc under [`docs/development/`](../docs/development/) and/or
      [`features/`](../features/).
- [ ] `docs/development/tracking.md` reflects the current status of the
      milestone this PR contributes to.
