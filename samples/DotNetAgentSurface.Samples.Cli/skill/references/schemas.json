{
  "add-task": {
    "type": "object",
    "properties": {
      "notes": {
        "type": "string",
        "nullable": true,
        "description": "Optional notes about the task."
      },
      "title": {
        "type": "string",
        "description": "The task title."
      }
    },
    "required": [
      "title"
    ],
    "additionalProperties": false
  },
  "complete-task": {
    "type": "object",
    "properties": {
      "id": {
        "type": "integer",
        "description": "The identifier of the task to complete."
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "list-tasks": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "remove-task": {
    "type": "object",
    "properties": {
      "id": {
        "type": "integer",
        "description": "The identifier of the task to remove."
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  }
}
