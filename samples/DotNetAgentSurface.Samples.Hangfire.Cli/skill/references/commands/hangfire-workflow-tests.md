# hangfire workflow tests

## `damage-sync-job`

Runs DamageSyncJob directly and captures its log transcript.

- Safety: `Confirm`
- Idempotent: `no`

### Parameters
- `--workflowOptions` (WorkflowJobOptions), required
